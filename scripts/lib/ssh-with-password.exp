#!/usr/bin/env expect
set timeout 180
log_user 1
set password [lindex $argv 0]
set command [lrange $argv 1 end]
eval spawn $command
expect {
  -re "(?i)(password:|passphrase for)" { send -- "$password\r"; exp_continue }
  eof
}
catch wait result
exit [lindex $result 3]
